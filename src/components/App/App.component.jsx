import { Header } from '../Shared/Header';
import { RenderRoutes as Routes } from '../../routes/RenderRoutes';

const App = () => (
  <div>
    <Header />
    <Routes />
  </div>
);

export default App;
